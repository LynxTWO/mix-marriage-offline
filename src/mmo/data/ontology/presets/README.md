# Presets In Ontology

This directory hosts canonical run-config presets under the ontology tree.

MMO resolves preset files from `ontology/presets/` when present, while keeping
the legacy top-level `presets/` directory for compatibility with existing
workflows and tests.
