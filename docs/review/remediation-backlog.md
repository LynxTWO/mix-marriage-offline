<!-- markdownlint-disable-file MD013 -->

# Remediation Backlog

This backlog is a synthesis pass built from current anti-dark-code evidence in
the repo. It does not claim full repo coverage beyond what
`docs/architecture/coverage-ledger.md` already supports.

Risk and likely impact use `High`, `Medium`, and `Low`.
Confidence uses `verified`, `inferred`, and `unknown`.
Approval values use `yes` and `no`.
Status values use `open`, `blocked`, `deferred`, and `ready`.

Recently completed on this branch:

- GUI stderr forwarding in `gui/lib/mmo_cli_runner.mjs` and
  `gui/lib/rpc_process_client.mjs` now uses allowlisted summaries backed by
  `cd gui && npm test`. Keep `docs/review/approval-packets.md` as the approval
  trail for that protected-area edit.
- Bundled plugin implementations and packaged plugin data now have a dedicated
  read-only review in `docs/review/bundled-plugin-review.md`. The coverage
  ledger moved that slice from `deferred` to `mapped`.
- `src/mmo/core/plugin_loader.py` and `src/mmo/core/plugin_market.py` now have
  targeted trust-boundary comments backed by
  `docs/review/bundled-plugin-trust-boundary-audit.md`.

## Safe to fix now

No open item currently fits this bucket. The bundled-plugin review completed on
this branch, and the next useful plugin follow-up now touches protected
authority or render paths.

## Approval-gated

| Title | Area or slice | Risk level | Likely impact | Why it matters | Evidence found | Confidence | Approval needed | Recommended next prompt or pass type | Smallest safe next step | Verification plan | Owner if known | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Clarify bundled-plugin authority split and shipped critical implementations | Bundled plugin implementations and packaged plugin data across `src/mmo/core/plugin_loader.py`, `src/mmo/core/plugin_market.py`, `src/mmo/plugins/`, `src/mmo/data/plugin_market/assets/plugins/`, `src/mmo/dsp/plugins/registry.py`, and `src/mmo/plugins/subjective/` | High | High | The bundled-plugin review mapped the real authority split, and the first protected comment batch clarified loader and market boundaries. The remaining high-value follow-up still touches shipped renderers, approval-gated corrective plugins, and a manifest-loader bypass. | `docs/review/bundled-plugin-review.md`; `docs/review/bundled-plugin-trust-boundary-audit.md`; `docs/unknowns/bundled-plugin-review.md`; `docs/architecture/coverage-ledger.md` row `Bundled plugin implementations and packaged plugin data`; `docs/architecture/repo-slices.md` slice `Bundled plugin implementations and packaged plugin data`; `docs/architecture/system-map.md` plugin-store row | verified | yes | approval-aware bundled-plugin comment pass or trust-boundary audit | Continue with smaller protected batches on `src/mmo/plugins/renderers/`, approval-gated corrective detectors and resolvers, and the subjective-pack bypass instead of reopening loader and market first. | `tools/run_pytest.sh -q tests/test_plugin_loader.py tests/test_plugin_market.py tests/test_renderer_runner.py`; rerun `python3 tools/validate_contracts.py`; rerun `npx --yes markdownlint-cli` on touched docs; spot-check that any new comments still match the current authority split. | not declared in repo | open |
| Split machine-readable project output from shared-log-safe output | Project and scan output surfaces in `src/mmo/cli_commands/_project.py` and `src/mmo/tools/scan_session.py` | High | High | `project show`, `project save`, `project load`, and scan output expose absolute project paths, session paths, stem paths, hashes, and media-tag data. Those are product outputs locally, but they become telemetry once wrappers, CI, or support capture stdout. | `docs/security/logging-audit.md` findings for `_project.py`, `src/mmo/tools/scan_session.py`, `src/mmo/core/session.py`, and `src/mmo/core/media_tags.py`; `docs/security/logging-audit.md` approval-status row `Project CLI JSON output in _project.py`; `docs/review/scenario-stress-test.md` section 5; `docs/architecture/coverage-ledger.md` row `Machine-readable product output and local trace escape` | verified | yes | project-output boundary review or approved redaction pass | Design a narrow boundary between machine-readable local output and shared-log-safe summaries before touching the CLI JSON contract. Keep the first edit on one output family instead of changing all JSON surfaces at once. | `tools/run_pytest.sh -q tests/test_cli_project_load_save.py tests/test_cli_project_show.py tests/test_scan_smoke.py tests/test_cli_scan_lfe_audit.py`; rerun `python tools/validate_contracts.py`; review one captured stdout sample to confirm path-bearing fields only move where intended. | CLI, tooling, or support maintainers | open |
| Narrow live render progress path output | Render live-progress logging in `src/mmo/cli_commands/_renderers.py` and `src/mmo/core/progress.py` | High | Medium | `[MMO-LIVE]` stderr output currently carries raw `where` values such as report, output, receipt, and QA paths. That is machine-local path data on a protected render path. | `docs/security/logging-audit.md` finding for `_renderers.py` and `progress.py`; `docs/security/logging-audit.md` approval-status row `Safe-render live progress path logging in _renderers.py`; `docs/review/adversarial-pass.md` protected-area list | verified | yes | render live-progress redaction pass | Replace absolute `where` path values with stable artifact labels or project-relative identifiers where the live-progress contract does not require full paths. | `tools/run_pytest.sh -q tests/test_cli_safe_render.py tests/test_safe_render_live_progress.py`; rerun `python tools/validate_contracts.py`; inspect one `[MMO-LIVE]` stderr sample from a render run. | not declared in repo | open |
| Narrow packaged smoke receipts and release workflow console output | Packaged smoke and release control plane in `tools/smoke_packaged_desktop.py` and `.github/workflows/release.yml` | High | Medium | The smoke harness emits path-rich JSON on success and raw installer stdout, stderr, and log tails on failure. The Windows release workflow also echoes install paths directly into CI logs. Both sit on protected support and control-plane paths. | `docs/security/logging-audit.md` findings for `tools/smoke_packaged_desktop.py` and `.github/workflows/release.yml`; `docs/security/logging-audit.md` approval-status rows for packaged smoke receipt output and Windows install path echoes; `docs/review/adversarial-pass.md` protected-area list and support-tool priority; `docs/architecture/coverage-ledger.md` row `Support tooling, smoke harnesses, and release control plane` | verified | yes | packaged-smoke and release-log redaction pass | First narrow the smoke harness output contract to artifact kind, verdict, and bounded status summaries, then remove raw install-path echoes from workflow console output. Keep full installer logs on disk or as opt-in artifacts only. | `python3 -m py_compile tools/smoke_packaged_desktop.py`; `tools/run_pytest.sh -q tests/test_packaged_desktop_smoke.py tests/test_packaged_smoke_goldens.py`; `python tools/validate_contracts.py`; review the release workflow diff for console-output shape before merge. | desktop packaging or release tooling | open |

## Needs more evidence

| Title | Area or slice | Risk level | Likely impact | Why it matters | Evidence found | Confidence | Approval needed | Recommended next prompt or pass type | Smallest safe next step | Verification plan | Owner if known | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Decide how checkout examples, packaged fallback manifests, offline market assets, and subjective bypasses fit one plugin-authority story | Bundled plugin surfaces in `plugins/`, `src/mmo/data/plugins/`, `src/mmo/data/plugin_market/assets/plugins/`, `src/mmo/plugins/`, and `src/mmo/plugins/subjective/` | High | High | The bundled-plugin review proved the split, but the repo still does not prove the intended long-term contract for checkout-only examples, market-asset parity, or the subjective-pack bypass. That blocks a broader plugin cleanup or hardening pass. | `docs/review/bundled-plugin-review.md`; `docs/unknowns/bundled-plugin-review.md`; `docs/architecture/coverage-ledger.md`; `docs/architecture/repo-slices.md` | verified | no | plugin-authority evidence pass | Resolve one boundary at a time: checkout examples, market-asset parity, or subjective bypass. Do not bundle those questions into one speculative fix. | Review plugin docs, plugin-market tests, and any release or packaging notes that define intended parity; if a later doc or code change lands, rerun the plugin-loader and plugin-market checks from the approval-gated backlog item. | not declared in repo | open |
| Confirm machine-readable output and trace escape channels | Machine-readable product output in `_project.py`, `src/mmo/tools/scan_session.py`, and `tools/agent/*` | High | High | The repo has verified path-bearing JSON and trace artifacts, but it still does not prove which CI, support, or issue-handling channels capture or forbid those outputs in practice. That gap blocks safe prioritization between redaction, documentation, and operator guidance. | `docs/review/scenario-stress-test.md` section 5; `docs/unknowns/adversarial-pass.md` row for `_project.py`, `scan_session.py`, and `tools/agent/*`; `docs/unknowns/scenario-stress-test.md`; `docs/unknowns/logging-audit.md` | unknown | no | output-boundary evidence pass | Trace the documented and out-of-repo channels that can capture CLI JSON and local trace artifacts before choosing a remediation shape. Keep this as an evidence-gathering pass, not a code change. | Review support docs, issue templates, and any artifact-upload guidance outside the repo; if a later code fix follows, reuse the verification plans from the project-output backlog item. | CLI, tooling, or support maintainers | open |
| Trace support-script audience and real call sites | Quiet support and control-plane scripts `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, and `tools/sync_claude_agents.py` | High | Medium | These scripts delete repo-local temp roots, rewrite packaged-data mirrors, or rewrite steering mirrors, but the current docs still do not prove how often they run in maintainer, CI, or release workflows. | `docs/review/adversarial-pass.md` gap on support and control-plane tools; `docs/review/scenario-stress-test.md` section 1; `docs/unknowns/adversarial-pass.md`; `docs/unknowns/scenario-stress-test.md`; `docs/architecture/coverage-ledger.md` row `Support tooling, smoke harnesses, and release control plane` | unknown | no | support-tool audience audit | Trace each script through repo docs, workflow jobs, and maintainer instructions. Decide whether each path needs its own architecture note, review slice, or no further action. | Confirm call sites in repo docs and workflow files; if a follow-up note or map change lands, run `npx --yes markdownlint-cli` on the touched docs. | tooling or release maintainers | open |
| Confirm public publish and Windows release boundaries | Pages publish path in `.github/workflows/pages.yml` and `site/`, plus Windows release and installer behavior in `.github/workflows/release.yml` and packaged smoke outputs | High | Medium | Public docs deploy and Windows release behavior both sit partly outside repo-owned runtime code. The repo confirms these boundaries exist, but it does not yet prove which parts are hand-maintained, generated, runner-dependent, or installer-dependent. | `docs/review/adversarial-pass.md` Pages and release-control-plane findings; `docs/review/scenario-stress-test.md` sections 3 and 4; `docs/unknowns/adversarial-pass.md`; `docs/unknowns/scenario-stress-test.md`; `docs/unknowns/maintenance-harness.md` | unknown | no | publish-and-release boundary audit | Separate the public publish boundary from the Windows release boundary in a follow-up evidence pass. Record what is repo-owned, what is GitHub or runner-owned, and what still depends on installer or signing runtime behavior. | Inspect repo docs, workflow jobs, and any available release dry-run notes; if a later release-path change follows, use the smoke and workflow verification plan already listed in the approval-gated backlog item. | docs or release maintainers | open |
| Evaluate `tools/agent/*` artifact-contract hardening | Agent trace and contract-stamp artifacts in `tools/agent/run.py`, `tools/agent/trace.py`, and `tools/agent/contract_stamp.py` | High | Medium | The path-bearing local artifacts are verified, but the current trace and stamp shape is also persisted, documented, and validated. That makes the open question a contract-boundary problem, not a small hardening fix. | `docs/security/logging-audit.md` finding for `tools/agent/*`; `docs/unknowns/logging-audit.md`; `docs/review/safe-fix-plan.md`; `docs/architecture/coverage-ledger.md` row `Machine-readable product output and local trace escape` | verified | no | artifact-contract audit or machine-readable output review | Determine whether any shared channel or support path needs a sanitized trace or stamp contract before changing the persisted artifact shape. | Review support and artifact-sharing practice outside the repo; if a later contract change is approved, run `tools/run_pytest.sh -q tests/test_agent_harness.py` and inspect fresh local trace and stamp samples. | agent harness | deferred |
| Confirm helper entrypoint audience and trusted-evidence role | Helper entrypoints `tools/run_renderers.py`, `tools/benchmark_render_precision.py`, and `tools/capture_tauri_screenshots.py` | Medium | Medium | These helpers bypass the main CLI and desktop entrypoints while still generating manifests, temp scenes, benchmarks, or screenshot evidence that maintainers may treat as trusted review output. | `docs/review/adversarial-pass.md` undercounted helper-path finding; `docs/review/scenario-stress-test.md` section 2; `docs/unknowns/adversarial-pass.md`; `docs/unknowns/scenario-stress-test.md`; `docs/architecture/coverage-ledger.md` support-tool row | unknown | no | helper-entrypoint audience audit | Document which audience owns each helper and whether its outputs count as trusted review evidence, CI-only evidence, or maintainer-only local output. | Confirm each helper’s audience in repo docs and workflow jobs; if a later doc or control-plane note lands, run `npx --yes markdownlint-cli` on the touched docs. | GUI or tooling maintainers | open |
| Confirm PR template enforcement outside the repo | GitHub PR workflow and branch-protection behavior around `.github/pull_request_template.md` | Medium | Low | The maintenance harness can enforce asset presence and wording, but it cannot prove that GitHub or branch protection forces contributors to fill the template before merge. | `docs/review/maintenance-harness.md` remaining limits; `docs/unknowns/maintenance-harness.md` PR-template row | unknown | no | branch-protection and review-policy check | Confirm whether any out-of-repo branch-protection, review policy, or maintainer habit treats template completion as required. If not, keep this as reviewer guidance rather than pretending it is enforced. | Review GitHub branch-protection and maintainer policy outside the repo; if repo docs change later, run `npx --yes markdownlint-cli docs/review/maintenance-harness.md docs/unknowns/maintenance-harness.md`. | not declared in repo | open |

## Coverage limits

The current repo evidence still leaves these holes outside repo-local proof:

- support scripts that mutate repo or packaged state without a fully documented
  maintainer or CI audience
- release tooling, Windows signing, packaged smoke, and installer behavior that
  depend on GitHub runners, Windows tooling, or installer state outside the repo
- GitHub Pages deploy behavior and any generation boundary behind `site/`
- machine-readable CLI JSON, NDJSON traces, and other local artifacts that
  become telemetry once wrappers, CI, or support workflows capture them
- steering mirrors under `.claude/agents/` that can look authoritative enough
  to distort review focus if the canonical source is not restated

No reviewed anti-dark-code evidence in this repo showed a sibling repo,
submodule-owned runtime, remote config service, feature-flag dashboard,
checked-in notebook workflow, admin tool, or migration path that currently
changes shipped behavior. This backlog therefore does not add remediation items
for those categories.
