# MMO Architecture Coverage Ledger

<!-- markdownlint-disable-file MD013 -->

This ledger tracks repo coverage for architecture and anti-dark-code passes.
Use it to show what has been mapped, what has already had comment passes, what
is deferred for later work, and what should stay excluded from inline-code
review.

Status words are narrow. `commented` means a targeted comment or docs pass
exists for that slice. It does not mean parallel support scripts, CI control
planes, or out-of-repo release steps are fully closed.

| Area or slice | Paths | Risk class | Status | Reason it matters | Evidence used | Likely owner if known | Next pass |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Steering and architecture docs | `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `README.md`, `PROJECT_WHEN_COMPLETE.md`, `docs/` | Medium | mapped | These files define repo promises, anti-dark-code rules, architecture docs, user manual, and current operator guidance. | Tracked top-level docs, `docs/README.md`, architecture docs, steering refresh pass | not declared in repo | Update when coverage or unknowns move. |
| Python CLI and command dispatch | `src/mmo/cli.py`, `src/mmo/cli_commands/`, `src/mmo/config.py`, `src/mmo/_frozen_cli_entrypoint.py` | Critical | mapped | This is the main operator entry surface for analysis, scene, render, project, plugin, and watch flows. | `src/mmo/cli.py`, GUI RPC inspection, system map, architecture pass | not declared in repo | Drill deeper only for a command-family audit. |
| Backend render and authority paths already commented | `src/mmo/resources.py`, selected `src/mmo/core/`, selected `src/mmo/cli_commands/` | Critical | commented | This slice owns resource precedence, plugin authority, render orchestration, gates, compare, project-session persistence, source resolution, and watch-folder behavior. | Completed critical-path comment passes, 2026-04-18 safe-render live-progress redaction pass, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Audit again only if behavior changes or a deeper security review is requested. |
| Analysis and intake backend: scan and planner inputs | `src/mmo/tools/scan_session.py`, `src/mmo/tools/analyze_stems.py`, `src/mmo/tools/run_pipeline.py`, `src/mmo/core/stems_index.py`, `src/mmo/core/stems_classifier.py`, `src/mmo/core/bus_plan.py` | Critical | commented | This slice turns raw stems into the scan receipt, applies pipeline mutations, and derives the classifier and bus-plan inputs used by later planner stages. | 2026-04-16 analysis-and-intake comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Revisit only if scan or planner behavior changes. |
| Analysis and intake backend: scene binding, precedence, and routing | `src/mmo/core/scene_builder.py`, `src/mmo/core/stems_draft.py`, `src/mmo/core/routing.py`, `src/mmo/core/precedence.py` | Critical | commented | This slice turns analyzed stems and bus assignments into scene objects or beds, applies lock precedence, and emits the report-side routing plan used by later render stages. | 2026-04-16 scene-binding comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, scene and precedence tests | not declared in repo | Revisit only if scene or lock behavior changes. |
| Analysis and intake backend: scene compatibility and primary renderer-side scene consumers | `src/mmo/core/profiles.py`, selected scene consumers in `src/mmo/cli_commands/_renderers.py` | Critical | commented | This slice turns scene metadata, locked scene payloads, and renderer-side recommendation precedence into final render and auto-apply eligibility decisions. | 2026-04-16 scene-consumer comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, scene and profile test references | not declared in repo | Revisit only if renderer-side scene consumers change. |
| Analysis and intake backend: remaining downstream scene consumers | selected `src/mmo/cli_commands/`, remaining `src/mmo/core/` scene helpers | Critical | deferred | Secondary report-rewrite and workflow surfaces still consume scene and routing state, but the higher-risk renderer-side authority path is now covered. | Repo tree, command inventory, current comment-pass scope | not declared in repo | Pick this up only if a later pass needs broader scene-consumer coverage. |
| DSP core: decode, resampling, downmix, transcode, export finalization, and recall export | `src/mmo/dsp/backends/ffmpeg_discovery.py`, `src/mmo/dsp/backends/ffprobe_meta.py`, `src/mmo/dsp/backends/ffmpeg_decode.py`, `src/mmo/dsp/decoders.py`, `src/mmo/dsp/sample_rate.py`, `src/mmo/dsp/downmix.py`, `src/mmo/dsp/transcode.py`, `src/mmo/dsp/export_finalize.py`, `src/mmo/exporters/recall_sheet.py` | Critical | commented | This slice owns tool resolution, non-WAV decode, canonical sample-rate selection, deterministic downmix and transcode behavior, PCM finalization, and the main CSV evidence artifact used to review render outputs. | 2026-04-16 DSP and recall-export comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, DSP and exporter test references | not declared in repo | Next: renderer plugins, render-audio, downmix QA, and richer exporters. |
| DSP and exporter follow-up consumers | `src/mmo/plugins/renderers/`, `src/mmo/core/render_run_audio.py`, `src/mmo/core/downmix_qa.py`, `src/mmo/dsp/channel_layout.py`, `src/mmo/exporters/pdf_report.py`, `src/mmo/exporters/pdf_manual.py` | Critical | commented | These paths own audio mutation, render-side FFmpeg use, channel-layout fallback, downmix QA evidence, and richer exported review surfaces beyond the core DSP contracts covered in the first DSP pass. | 2026-04-16 render-execution and downmix-QA, PDF report exporter, corrective-renderer, recall-and-downmix-exporter, and PDF manual exporter passes; `docs/unknowns/critical-paths.md`; system map | not declared in repo | Revisit only if render, QA, or exporter behavior changes. |
| Packaged contracts and registry authority | `schemas/`, `ontology/`, `src/mmo/data/` | Critical | mapped | Contract authority and packaged data decide runtime meanings, status IDs, plugin bundles, and policy references. | `src/mmo/resources.py`, schema and ontology layout, validation workflows, prior architecture map | not declared in repo | Audit when contract or migration work is in scope. |
| Plugin contracts and authoring surfaces | `src/mmo/plugins/interfaces.py`, `src/mmo/plugins/runtime_contract.py`, `examples/plugin_authoring/` | High | commented | Shared plugin contracts, purity guardrails, and authoring examples shape how third-party or local plugin code is expected to behave. | 2026-04-16 plugin ecosystem pass, plugin test references, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Revisit only if plugin contracts or authoring guidance changes. |
| Bundled plugin implementations and packaged plugin data | `plugins/`, `src/mmo/data/plugins/`, `src/mmo/plugins/`, `src/mmo/data/plugin_market/assets/plugins/`, `src/mmo/dsp/plugins/registry.py`, `src/mmo/plugins/subjective/` | High | commented | Bundled plugin behavior is split across repo manifests, packaged fallback manifests, shipped implementation modules, offline market assets, and a subjective-pack bypass outside the main manifest loader. Loader, market, renderer, corrective, and subjective-bypass trust boundaries now have targeted notes. Remaining questions are evidence gaps about checkout examples and offline market parity, not missing trust-boundary commentary on the main shipped code paths. | `docs/review/bundled-plugin-review.md`, `docs/review/bundled-plugin-trust-boundary-audit.md`, `docs/review/bundled-renderer-comment-audit.md`, `docs/review/bundled-corrective-plugin-audit.md`, `docs/review/bundled-subjective-bypass-audit.md`, repo plugin roots, packaged-data mirror tooling, system map | not declared in repo | Revisit only for checkout-example visibility or offline market parity evidence work, or if bundled plugin authority changes again. |
| Local dev-shell bridge | `gui/server.mjs`, `gui/lib/mmo_cli_runner.mjs`, `gui/lib/rpc_process_client.mjs` | High | commented | This bridge exposes the local HTTP surface, CLI runner fallback, RPC subprocess handoff, and artifact-read allowlists. | 2026-04-16 dev-shell bridge pass, 2026-04-17 GUI stderr redaction pass, GUI bridge tests, logging-audit refresh, system map | not declared in repo | Revisit only if the local shell surface changes again or a fresh logging audit finds a new bridge leak. |
| Local dev-shell frontend | `gui/web/`, browser-side `gui/lib/*.mjs`, `gui/tests/`, `gui/package.json` | High | commented | The browser frontend shapes local operator behavior, project hydration, scene-lock editing, artifact views, and audition preview. | 2026-04-16 browser-state dev-shell pass, browser-helper pass, GUI tests, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Revisit only if browser state or helper authority changes. |
| Packaged desktop frontend | `gui/desktop-tauri/src/`, `gui/desktop-tauri/layouts/`, `gui/desktop-tauri/tests/`, `gui/desktop-tauri/package.json` | High | commented | This is the shipped GUI surface for the offline app and now has targeted comment coverage for state entrypoints, sidecar handoff, compare QA derivation, and recent-path handling. | 2026-04-16 packaged desktop frontend pass, desktop tests, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Revisit only if packaged frontend state ownership changes. |
| Desktop native shell and sidecar packaging | `gui/desktop-tauri/src-tauri/src/`, `gui/desktop-tauri/src-tauri/Cargo.toml`, `gui/desktop-tauri/src-tauri/tauri.conf.json`, `gui/desktop-tauri/src-tauri/binaries/`, desktop packaging scripts | High | commented | Native commands, sidecar discovery, bundled binaries, and packaging assumptions are high-risk and now have targeted comment coverage around smoke-only commands, sidecar staging, and packaged smoke contracts. | 2026-04-16 desktop native-shell and smoke pass, packaged smoke tests, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Revisit only if sidecar or packaging behavior changes. |
| Validation runners and contract checks | `tools/validate.py`, `tools/run_pytest.*`, `tools/build_binaries.py`, `tools/validate_packaged_data_mirror.py`, `.github/workflows/ci.yml`, `.github/workflows/policy-validation.yml`, `Makefile` | High | commented | These paths define the repo-safe validation commands, binary build fallback notes, and contract or policy CI lanes that later reviews rely on as evidence. | 2026-04-16 validation-and-release-tooling pass, pytest runner fix, workflow review, system map | not declared in repo | Revisit if validation entrypoints or CI lanes change. |
| Support tooling, smoke harnesses, and release control plane | `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py`, `tools/run_renderers.py`, `tools/capture_tauri_screenshots.py`, `tools/benchmark_render_precision.py`, `tools/smoke_packaged_desktop.py`, `.github/workflows/release.yml` | High | mapped | These scripts and workflows act like quiet control planes. They delete, sync, benchmark, stage screenshots, smoke installers, or expose release-signing state outside the main CLI and desktop runtime maps. | Adversarial pass review, logging audit, workflow inspection, support-tool source review | not declared in repo | Run a targeted support-tool and release-control-plane audit before claiming end-to-end closure. |
| Public docs publish and docs deploy path | `site/`, `.github/workflows/pages.yml` | High | mapped | The repo owns a public publish path for operator-facing docs through GitHub Pages. That is shipped behavior outside the local app runtime even when the site content itself is static. | Scenario stress-test pass, workflow inspection, system map, adversarial pass review | not declared in repo | Revisit when site generation or Pages deploy behavior changes. |
| Machine-readable product output and local trace escape | `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*`, logging-audit docs | High | mapped | CLI JSON, NDJSON traces, and path-bearing local receipts are product output on the local machine, but they become telemetry once wrappers, CI, or support workflows capture them. `project show` now has an additive shared-log-safe JSON mode, but the GUI still depends on the full local payload and the wider scan and trace boundary is still open. | Logging audit, scenario stress-test pass, adversarial pass, workflow inspection, phase-1 `project show` shared-output pass | not declared in repo | Revisit default `project show` use, `project save` and `project load`, scan output, and agent-artifact sharing before treating this slice as closed. |
| Tests and fixtures | `tests/`, `fixtures/`, `pytest.ini` | Medium | mapped | These are the main evidence sources for runtime behavior, deterministic artifacts, and regression expectations. | Fixture README, test tree, pytest config, workflow references, critical-path pass evidence | not declared in repo | Audit only when coverage claims need stronger proof. |
| Examples and benchmarks | `examples/`, `benchmarks/` | Medium | mapped | These paths explain authoring, benchmark expectations, and reference material, but they should not be mistaken for core runtime code. | Repo tree, tracked example files, `benchmarks/README.md` | not declared in repo | Revisit only if they become misleading or stale. |
| Generated frontend dependencies | `gui/desktop-tauri/node_modules/` | Low | excluded | Installed frontend dependencies are not repo-owned implementation surfaces and should not receive inline comment passes. | Directory structure, absence from `git ls-files`, local package manifests | third-party dependencies | Explain through package manifests and lockfiles. |
| Built desktop frontend output | `gui/desktop-tauri/dist/` | Low | excluded | Built frontend assets are output artifacts, not first-party source of truth. | Directory structure, absence from `git ls-files`, desktop build layout | generated build output | Explain through source files and build config. |
| Generated Tauri metadata | `gui/desktop-tauri/src-tauri/gen/` | Low | excluded | Generated Tauri metadata is machine output and should not become a comment-pass target. | Directory structure, absence from `git ls-files`, Tauri tree inspection | generated build output | Explain through Tauri config and source manifests. |
| Rust build output | `gui/desktop-tauri/src-tauri/target/` | Low | excluded | Cargo build output is not repo-owned architecture. | Directory structure, absence from `git ls-files`, Tauri tree inspection | generated build output | Explain through Rust source, Cargo manifest, and build workflows. |
| Vendored Rust patch | `gui/desktop-tauri/src-tauri/vendor/glib-0.18.5/` | Low | excluded | This tracked vendored patch affects builds, but it is third-party code and should not be treated as first-party MMO runtime logic. | Vendored path, tracked files, `Cargo.toml` patch reference | third-party vendored dependency | Explain through `MMO_VENDOR_NOTES.md`, Cargo patch notes, and release docs. |
