# MMO Architecture Coverage Ledger

<!-- markdownlint-disable-file MD013 -->

This ledger tracks repo coverage for architecture and anti-dark-code passes.
Use it to show what has been mapped, what has already had comment passes, what
is deferred for later work, and what should stay excluded from inline-code
review.

| Area or slice | Paths | Risk class | Status | Reason it matters | Evidence used | Likely owner if known | Next pass |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Steering and architecture docs | `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `README.md`, `PROJECT_WHEN_COMPLETE.md`, `docs/` | Medium | mapped | These files define repo promises, anti-dark-code rules, architecture docs, user manual, and current operator guidance. | Tracked top-level docs, `docs/README.md`, architecture docs, steering refresh pass | not declared in repo | Keep aligned as later slices change coverage or unknowns. |
| Python CLI and command dispatch | `src/mmo/cli.py`, `src/mmo/cli_commands/`, `src/mmo/config.py`, `src/mmo/_frozen_cli_entrypoint.py` | Critical | mapped | This is the main operator entry surface for analysis, scene, render, project, plugin, and watch flows. | `src/mmo/cli.py`, GUI RPC inspection, system map, architecture pass | not declared in repo | Extend only when a later pass needs deeper family-by-family audit coverage. |
| Backend render and authority paths already commented | `src/mmo/resources.py`, selected `src/mmo/core/`, selected `src/mmo/cli_commands/` | Critical | commented | This slice owns resource precedence, plugin authority, render orchestration, gates, compare, project-session persistence, source resolution, and watch-folder behavior. | Completed critical-path comment passes, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Narrow follow-up audit only if behavior changes or a deeper security review is requested. |
| Analysis and intake backend: scan and planner inputs | `src/mmo/tools/scan_session.py`, `src/mmo/tools/analyze_stems.py`, `src/mmo/tools/run_pipeline.py`, `src/mmo/core/stems_index.py`, `src/mmo/core/stems_classifier.py`, `src/mmo/core/bus_plan.py` | Critical | commented | This slice turns raw stems into the scan receipt, applies pipeline mutations, and derives the classifier and bus-plan inputs used by later planner stages. | 2026-04-16 analysis-and-intake comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, system map | not declared in repo | Stay in the same risk class and move next to scene binding and report-shaping flows. |
| Analysis and intake backend: scene binding, precedence, and routing | `src/mmo/core/scene_builder.py`, `src/mmo/core/stems_draft.py`, `src/mmo/core/routing.py`, `src/mmo/core/precedence.py` | Critical | commented | This slice turns analyzed stems and bus assignments into scene objects or beds, applies lock precedence, and emits the report-side routing plan used by later render stages. | 2026-04-16 scene-binding comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, scene and precedence tests | not declared in repo | Keep covered unless behavior changes or a deeper scene audit is requested. |
| Analysis and intake backend: scene compatibility and primary renderer-side scene consumers | `src/mmo/core/profiles.py`, selected scene consumers in `src/mmo/cli_commands/_renderers.py` | Critical | commented | This slice turns scene metadata, locked scene payloads, and renderer-side recommendation precedence into final render and auto-apply eligibility decisions. | 2026-04-16 scene-consumer comment pass, repo tree, module inspection, `docs/unknowns/critical-paths.md`, scene and profile test references | not declared in repo | Move next to the critical DSP and exporter slice unless render-side scene consumers change again. |
| Analysis and intake backend: remaining downstream scene consumers | selected `src/mmo/cli_commands/`, remaining `src/mmo/core/` scene helpers | Critical | deferred | Secondary report-rewrite and workflow surfaces still consume scene and routing state, but the higher-risk renderer-side authority path is now covered. | Repo tree, command inventory, current comment-pass scope | not declared in repo | Revisit only if a later pass needs broader scene-consumer coverage beyond the main renderer path. |
| DSP, render kernels, and exporters | `src/mmo/dsp/`, `src/mmo/exporters/` | Critical | deferred | These modules own irreversible audio side effects, decode and render behavior, and exported evidence artifacts. | Repo tree, module inventory, package docs, system map | not declared in repo | Run an architecture or comment pass focused on DSP trust boundaries and exporter failure modes. |
| Packaged contracts and registry authority | `schemas/`, `ontology/`, `src/mmo/data/` | Critical | mapped | Contract authority and packaged data decide runtime meanings, status IDs, plugin bundles, and policy references. | `src/mmo/resources.py`, schema and ontology layout, validation workflows, prior architecture map | not declared in repo | Run an ontology-contract or migration-discipline audit when contract work is in scope. |
| Plugin ecosystem and authoring surfaces | `src/mmo/plugins/`, `plugins/`, `src/mmo/data/plugins/`, `examples/plugin_authoring/` | High | mapped | Plugin interfaces, bundled fallback plugins, and authoring examples can change render and UI behavior. | Plugin loader and market review, repo plugin roots, authoring examples, system map | not declared in repo | Run a plugin-contract or plugin-comment pass when extension behavior is the focus. |
| Local dev-shell bridge | `gui/server.mjs`, `gui/lib/` | High | mapped | This bridge exposes the local HTTP surface, CLI runner, RPC handoff, and artifact-read allowlists. | `gui/server.mjs`, `gui/lib/mmo_cli_runner.mjs`, system map | not declared in repo | Run a frontend or bridge audit only if the local shell surface changes. |
| Local dev-shell frontend | `gui/web/`, `gui/tests/`, `gui/package.json` | High | deferred | The browser frontend shapes local operator behavior, but current coverage stops at entrypoints and bridge wiring. | GUI tree, `package.json`, local server inspection, system map | not declared in repo | Run a frontend architecture pass to map state, screens, and browser-side trust edges. |
| Packaged desktop frontend | `gui/desktop-tauri/src/`, `gui/desktop-tauri/layouts/`, `gui/desktop-tauri/tests/`, `gui/desktop-tauri/package.json` | High | deferred | This is the shipped GUI surface for the offline app, but current coverage is still entrypoint and packaging deep rather than state deep. | Desktop tree, package manifest, layout files, test list, GUI docs, system map | not declared in repo | Run a packaged-frontend architecture pass before any inline comment or audit pass here. |
| Desktop native shell and sidecar packaging | `gui/desktop-tauri/src-tauri/src/`, `gui/desktop-tauri/src-tauri/Cargo.toml`, `gui/desktop-tauri/src-tauri/tauri.conf.json`, `gui/desktop-tauri/src-tauri/binaries/`, desktop packaging scripts | High | deferred | Native commands, sidecar discovery, bundled binaries, and packaging assumptions are high-risk and only partly mapped. | Rust entry files, Cargo manifest, Tauri config, release and smoke workflows, system map | not declared in repo | Run a native-shell or trust-boundary audit when desktop packaging or sidecar behavior changes. |
| Validation, smoke, and release tooling | `tools/`, `Makefile`, `.github/workflows/`, repo pytest runners | High | mapped | These paths define safe validation commands, smoke flows, release steps, signing surfaces, and operator expectations. | `Makefile`, validation scripts, smoke harness, workflow files, architecture map | not declared in repo | Run an ops or release audit if reproducibility, signing, or CI drift becomes a concern. |
| Tests and fixtures | `tests/`, `fixtures/`, `pytest.ini` | Medium | mapped | These are the main evidence sources for runtime behavior, deterministic artifacts, and regression expectations. | Fixture README, test tree, pytest config, workflow references, critical-path pass evidence | not declared in repo | Run an evidence-quality or fixture-governance pass only when coverage claims need stronger proof. |
| Examples, benchmarks, and published outputs | `examples/`, `benchmarks/`, `site/` | Medium | mapped | These paths explain authoring, UI states, benchmarking, and the published site surface, but they should not be mistaken for core runtime code. | Repo tree, tracked example files, `benchmarks/README.md`, `site/` contents | not declared in repo | Revisit only when examples, benchmark contracts, or published site outputs become misleading or stale. |
| Generated frontend dependencies | `gui/desktop-tauri/node_modules/` | Low | excluded | Installed frontend dependencies are not repo-owned implementation surfaces and should not receive inline comment passes. | Directory structure, absence from `git ls-files`, local package manifests | third-party dependencies | Explain via package manifests and lockfiles, not by editing installed dependencies. |
| Built desktop frontend output | `gui/desktop-tauri/dist/` | Low | excluded | Built frontend assets are output artifacts, not first-party source of truth. | Directory structure, absence from `git ls-files`, desktop build layout | generated build output | Explain via source files and build config, not built assets. |
| Generated Tauri metadata | `gui/desktop-tauri/src-tauri/gen/` | Low | excluded | Generated Tauri metadata is machine output and should not become a comment-pass target. | Directory structure, absence from `git ls-files`, Tauri tree inspection | generated build output | Explain via Tauri config and source manifests. |
| Rust build output | `gui/desktop-tauri/src-tauri/target/` | Low | excluded | Cargo build output is not repo-owned architecture. | Directory structure, absence from `git ls-files`, Tauri tree inspection | generated build output | Explain via Rust source, Cargo manifest, and build workflows. |
| Vendored Rust patch | `gui/desktop-tauri/src-tauri/vendor/glib-0.18.5/` | Low | excluded | This tracked vendored patch affects builds, but it is third-party code and should not be treated as first-party MMO runtime logic. | Vendored path, tracked files, `Cargo.toml` patch reference | third-party vendored dependency | Explain via `MMO_VENDOR_NOTES.md`, Cargo patch notes, and release docs. |
