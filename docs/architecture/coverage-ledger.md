# MMO Architecture Coverage Ledger

<!-- markdownlint-disable-file MD013 -->

This ledger tracks repo coverage for architecture and anti-dark-code passes.
Use it to show what has been scanned, what is mapped well enough to support
later work, and what remains deferred or excluded.

| Area or path | Repo type or runtime unit | Risk class | Status | Why it matters | Evidence used | Likely owner if known | Next pass or blocking reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `src/mmo/cli.py`, `src/mmo/cli_commands/` | Python CLI and local RPC entry surfaces | Critical | mapped | This is the main operator entry surface for analysis, scene, render, project, plugin, and watch flows. | `src/mmo/cli.py`, `src/mmo/cli_commands/_gui_rpc.py`, existing system map, critical-path comment passes | not declared in repo | Extend only when a later pass needs deeper family-by-family audit coverage. |
| `src/mmo/core/` | Python backend orchestration, artifact contracts, render, compare, gates, watch, session, and source resolution | Critical | mapped | This area owns most workflow semantics, artifact integrity, and trust boundaries. | Prior architecture map, critical-path comment passes, core module inspection, validation tooling references | not declared in repo | Continue narrower audits for remaining analysis and scene-intake slices. |
| `src/mmo/resources.py`, `src/mmo/data/`, `schemas/`, `ontology/` | Packaged data resolution and contract authority | Critical | mapped | Contract and packaged-data authority determines what runtime rules the app follows. | `src/mmo/resources.py`, schema and ontology layout, `src/mmo/data/`, validation docs and workflows | not declared in repo | Revisit if a future pass needs deeper ontology migration or packaged-data mirror coverage. |
| `src/mmo/plugins/`, `plugins/`, `src/mmo/data/plugins/`, `examples/plugin_authoring/` | Plugin runtime, packaged fallback, and authoring surfaces | High | mapped | Plugin precedence and install roots can change render and UI behavior. | Plugin loader and market code, packaged plugin data, repo plugin roots, authoring example paths | not declared in repo | Extend if a later pass needs deeper plugin UI or authoring-contract coverage. |
| `gui/server.mjs` | Local web dev shell server | High | mapped | This file exposes the local HTTP surface, RPC bridge, and artifact read allowlists. | `gui/server.mjs`, existing system map, RPC docs, allowlist behavior | not declared in repo | Revisit only if dev-shell behavior changes or a deeper local-browser audit is needed. |
| `gui/` | Local web dev shell frontend assets and helpers | Medium | scanned | The dev shell shapes local operator behavior, but this pass only verified the entry and bridge surfaces. | `gui/package.json`, `gui/lib/`, `gui/web/`, `gui/server.mjs` | not declared in repo | Deeper frontend state and component mapping is deferred until a UI-focused pass. |
| `gui/desktop-tauri/` | Desktop frontend and packaging surface | High | scanned | This is the shipped GUI surface for the offline app. | `gui/desktop-tauri/package.json`, README, docs, Tauri config, smoke and screenshot workflows | not declared in repo | Deeper frontend state ownership is deferred. Current coverage stops at documented entrypoints and packaging. |
| `gui/desktop-tauri/src-tauri/` | Native shell, sidecar packaging, smoke command surface | High | scanned | Native packaging and bundled sidecar behavior are high-risk, but this pass stayed at config and entrypoint depth. | `Cargo.toml`, `tauri.conf.json`, `src/lib.rs`, release and smoke workflows | not declared in repo | Deeper Rust command and native-shell audit is deferred until needed. |
| `tools/`, `Makefile`, pytest runners | Validation, packaging, smoke, and operator tooling | High | mapped | These paths define safe validation commands, build outputs, and packaged-app smoke coverage. | `Makefile`, `tools/validate_contracts.py`, pytest runners, build and smoke scripts | not declared in repo | Extend if a future pass needs release-tool failure-mode or reproducibility depth. |
| `.github/workflows/` | CI, release, docs deploy, and signing orchestration | High | mapped | These workflows define cross-platform validation, publication, signing, and docs deployment. | `.github/workflows/ci.yml`, `policy-validation.yml`, `release.yml`, `pages.yml` | not declared in repo | Revisit when workflow topology or signing rules change. |
| `tests/`, `fixtures/`, smoke surfaces | Evidence sources for runtime behavior and shipped workflows | Medium | scanned | Tests and smoke artifacts show which contracts and workflows the repo treats as stable. | Repo pytest runners, screenshot flows, packaged smoke harness, fixture tools referenced by CI | not declared in repo | Deeper mapping is deferred until a test-coverage or evidence-quality pass. |
| `gui/desktop-tauri/src-tauri/vendor/glib-0.18.5/` | Vendored third-party Rust dependency patch | Low | excluded | It affects builds, but it is not repo-owned MMO architecture and should not be treated as first-party product logic. | Vendored path and `Cargo.toml` patch reference | third-party vendored dependency | Keep excluded unless the vendor patch itself becomes the subject of a dedicated maintenance pass. |
