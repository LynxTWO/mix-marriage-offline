# Scenario Stress-Test Unknowns

<!-- markdownlint-disable-file MD013 -->

This file keeps the gaps that still need proof after the scenario stress test.
Add rows here instead of smoothing over support tooling, publish control
planes, or machine-readable output boundaries that the repo docs still cannot
fully prove.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py` | It is still unclear which maintainer, CI, or release workflows rely on these scripts in practice | These scripts delete or rewrite repo-local state and steering mirrors, so the difference between maintainer-only tooling and routine control-plane behavior still matters | Static review shows explicit delete or copy side effects, but the current docs do not trace all call sites or human runbooks | tooling or release maintainers | Trace each script through docs, workflow jobs, and maintainer instructions, then decide whether it needs its own architecture note | high |
| `tools/run_renderers.py`, `tools/benchmark_render_precision.py`, `tools/capture_tauri_screenshots.py` | The repo docs still do not say whether these helper entrypoints are CI-only, maintainer-only, or operator-facing | They can generate manifests, temp scenes, benchmark receipts, or committed screenshot evidence outside the main CLI and desktop entrypoints | Static review shows direct imports of validator or CLI code plus writes to temp roots or committed screenshot baselines | GUI or tooling maintainers | Document which audience owns each helper and whether its outputs count as trusted review evidence | medium |
| `.github/workflows/pages.yml`, `site/` | It is still unclear whether any part of the published site is generated or refreshed outside the tracked repo flow | That boundary determines whether Pages deploy is only a static publish path or a broader public docs control plane | Static review confirms the deploy job exists, but the repo docs do not yet explain how `site/` is produced or refreshed | docs or release maintainers | Confirm whether `site/` is fully hand-maintained or generated elsewhere, then record that boundary | medium |
| `.github/workflows/release.yml`, `tools/smoke_packaged_desktop.py` | The repo still cannot prove exact Windows signing, installer, and receipt behavior from code alone | Release correctness depends on out-of-repo runner state, Windows cert-store behavior, `signtool`, and installer output | Static review plus the logging audit show protected signing steps and path-rich installer receipts, but not a sanitized end-to-end release proof | desktop packaging or release tooling | Capture a Windows release dry run with sanitized receipts and separate repo-owned facts from runner-only behavior | high |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | It is still unclear which support or CI channels capture or forbid machine-readable output and local trace artifacts | That boundary decides when local product output becomes shared telemetry and whether path-bearing traces stay local-only in practice | The logging audit found path-rich JSON and trace files, but no repo-owned workflow that clearly exports or forbids them | CLI, tooling, or support maintainers | Review support docs, issue templates, and artifact-upload habits that live outside the repo, then decide whether more redaction or documentation is needed | medium |
| `docs/claude_agents/`, `.claude/agents/`, `tools/sync_claude_agents.py` | The current docs say which copy is canonical, but they do not yet say how reviewers should treat workspace mirrors in coverage or comment passes | A mirrored path can look authoritative enough to pull review effort away from the real source of truth | `CLAUDE.md` names `docs/claude_agents/` as canonical and `.claude/agents/` as a synced workspace copy, while the sync tool rewrites the mirror | tooling or docs maintainers | Add one short note in the next steering or review pass that mirrored agent copies are workspace convenience, not primary review targets | medium |
