<!-- markdownlint-disable-file MD013 -->

# Remediation Pass Unknowns

This file records confidence gaps that still block a safe remediation pass.
Add rows here instead of smoothing over out-of-repo control planes, unclear
support-tool audiences, or unclear output-escape channels.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `plugins/`, `src/mmo/data/plugins/`, `src/mmo/data/plugin_market/assets/plugins/`, `src/mmo/plugins/`, and `src/mmo/plugins/subjective/` | Bundled plugin behavior is split across checkout manifests, packaged fallback manifests, offline market assets, shipped implementation modules, and a subjective-pack bypass outside the main loader | That split blocks any broad plugin cleanup or comment pass that pretends there is one bundled authority surface | `docs/review/bundled-plugin-review.md` mapped the authority split and `docs/unknowns/bundled-plugin-review.md` captured the still-open checkout-example, market-parity, and subjective-bypass questions | plugin, DSP, or release maintainers | Resolve one boundary at a time before planning edits, starting with loader and market authority notes rather than a broad implementation pass | High |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, and `tools/sync_claude_agents.py` | Repo-local docs and git history now support human-run maintainer or contributor use for these scripts, but the repo still cannot rule out an unwritten bootstrap, wrapper, or release-prep routine outside versioned files | That gap decides whether these paths stay as explicit maintainer tools or need stronger operational review rules later | The evidence pass now ties the three scripts to written maintainer or contributor docs, finds no direct CI or release invocation, and adds git-history proof that the scripts were introduced and maintained as human-run tools rather than workflow steps | tooling or release maintainers | Treat them as maintainer tools unless a future setup helper, bootstrap note, or workflow starts calling them directly | High |
| `tools/run_renderers.py` and `tools/benchmark_render_precision.py` | Git history now supports maintainer-local helper status for these entrypoints, but the repo still does not prove any broader trusted-output role | They can generate manifests, temp scenes, and benchmark receipts outside the main CLI and desktop entrypoints, and reviewers still need to know whether those outputs stay local or become review evidence later | The evidence pass now proves `capture_tauri_screenshots.py` is a CI artifact path and a maintainer baseline-refresh tool, while git history for the renderer helper and the precision benchmark shows no later workflow, release, or reviewer-facing promotion | GUI or tooling maintainers | Leave those two helpers on the maintainer-local side unless a future workflow or README names them as trusted review evidence | Medium |
| `.github/workflows/pages.yml` and `site/` | The repo now proves a committed static site that `pages.yml` uploads as-is, but it still cannot prove GitHub Pages hosting, serving, or caching behavior from repo code alone | The public docs path is shipped operator-facing behavior, so the repo needs to stay honest about what it owns and what GitHub owns after upload | The publish-boundary evidence pass now ties `pages.yml`, the three-file `site/` tree, and direct-edit git history together as repo-owned facts. The remaining gap is hosted Pages behavior, not missing repo-local generation proof. | docs or release maintainers | Reopen this only if the repo adds a site generator, a build step, or a different deploy path | Medium |
| `.github/workflows/release.yml` and Windows installer or signing behavior | The repo now proves the release workflow order, artifact assembly, smoke orchestration, and cleanup policy, but it still cannot prove the full Windows signing and installer runtime boundary from repo code alone | Signing, installer behavior, and hosted release semantics still depend on GitHub-hosted runners, Windows tooling, certificate stores, timestamping, and installer state outside the repo | The publish-and-release evidence pass now ties `release.yml`, `tools/smoke_packaged_desktop.py`, targeted tests, and git history together as repo-owned facts. The remaining gap is runner, signer, installer, and hosted-release behavior. | docs or release maintainers | Keep the Windows release boundary open until a sanitized dry run or equivalent hosted-run evidence narrows the out-of-repo side further | High |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | The repo now proves one local browser-visible `project.show` path, one generic issue-report channel, no repo-owned workflow upload for these outputs, and shared-safe shell defaults for project and scan stdout, but it still does not prove how support or CI handles the explicit local `json-local`, file-backed scan reports, or local trace artifacts in practice | That gap decides when local product output becomes shared telemetry and how much further redaction is safe without breaking intentional local contracts | `docs/security/logging-audit.md`, `docs/unknowns/logging-audit.md`, `docs/unknowns/adversarial-pass.md`, `docs/unknowns/scenario-stress-test.md`, and the evidence-gap pass all confirm the path-bearing outputs. GUI RPC inspection proves the local browser path, the bug template asks for exact commands and artifact paths, workflow inspection found no upload path for these outputs, and the current branch now defaults shell-facing project and scan stdout to shared-safe profiles. | CLI, tooling, or support maintainers | Review any remaining repo-local support guidance, then treat the unresolved capture story as an out-of-repo workflow question before choosing whether the explicit local contracts should narrow further | High |
| `tools/agent/run.py`, `tools/agent/trace.py`, `tools/agent/contract_stamp.py` | The path-bearing artifacts are verified, documented, validated, gitignored, and not uploaded by repo-owned workflows, but the repo still does not prove which shared human channels, if any, require a sanitized trace or stamp contract | The current trace and stamp shape is persisted, documented, and validated. That makes any future hardening change a contract-boundary decision, not a local cleanup. | `docs/security/logging-audit.md` confirmed path-bearing trace and stamp fields, `docs/review/safe-fix-plan.md` recorded that the current artifact shape is already documented and validated, and the evidence-gap pass confirmed the local-artifact defaults and lack of repo-owned uploads | agent harness | Confirm whether any support or review habit pastes these files into issue threads or PRs before planning a change to the persisted trace or stamp format | High |
| `.github/pull_request_template.md` and out-of-repo branch protection or review policy | Template completion may still be reviewer-only even after the maintenance harness lands | The repo can validate the template text, but it cannot prove GitHub enforces completion before merge | `docs/review/maintenance-harness.md` and `docs/unknowns/maintenance-harness.md` both state that template completion is not hard-enforced in the repo | not declared in repo | Confirm whether branch protection, review policy, or maintainer workflow outside the repo treats template completion as required | Medium |
