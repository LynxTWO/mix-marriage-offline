<!-- markdownlint-disable-file MD013 -->

# Remediation Pass Unknowns

This file records confidence gaps that still block a safe remediation pass.
Add rows here instead of smoothing over out-of-repo control planes, unclear
support-tool audiences, or unclear output-escape channels.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py` | The repo still does not prove which maintainer, CI, or release workflows rely on these scripts in practice | These scripts delete or rewrite repo-local state, packaged-data mirrors, and steering mirrors. That is control-plane behavior, not harmless glue code. | `docs/review/adversarial-pass.md`, `docs/review/scenario-stress-test.md`, `docs/unknowns/adversarial-pass.md`, and `docs/unknowns/scenario-stress-test.md` all call out missing audience and call-site proof | tooling or release maintainers | Trace each script through repo docs, workflow jobs, and maintainer instructions before planning a fix | High |
| `tools/run_renderers.py`, `tools/benchmark_render_precision.py`, `tools/capture_tauri_screenshots.py` | The helper entrypoints still do not have a proven audience or trusted-output role | They can generate manifests, temp scenes, benchmark receipts, or committed screenshot evidence outside the main CLI and desktop entrypoints | `docs/review/adversarial-pass.md`, `docs/review/scenario-stress-test.md`, `docs/unknowns/adversarial-pass.md`, and `docs/unknowns/scenario-stress-test.md` all flag the audience gap | GUI or tooling maintainers | Confirm whether each helper is CI-only, maintainer-only, or operator-facing, then document whether its output counts as trusted review evidence | Medium |
| `.github/workflows/pages.yml`, `site/`, `.github/workflows/release.yml`, and Windows installer or signing behavior | The repo still cannot prove the full public-publish and Windows release boundaries from repo code alone | Pages deploy, signing, smoke, and installer behavior depend on GitHub-hosted runners, GitHub Pages, Windows tooling, or installer state outside the repo | `docs/review/adversarial-pass.md`, `docs/review/scenario-stress-test.md`, `docs/unknowns/adversarial-pass.md`, `docs/unknowns/scenario-stress-test.md`, and `docs/unknowns/maintenance-harness.md` all mark these as out-of-repo boundaries | docs or release maintainers | Capture boundary notes that separate repo-owned facts from runner-, Pages-, signer-, or installer-owned behavior | High |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | It is still unclear which CI, support, or issue-handling channels capture or forbid machine-readable JSON and local trace artifacts | That gap decides when local product output becomes shared telemetry and how much redaction is safe without breaking automation | `docs/security/logging-audit.md`, `docs/unknowns/logging-audit.md`, `docs/unknowns/adversarial-pass.md`, and `docs/unknowns/scenario-stress-test.md` all confirm the path-bearing outputs but not the capture channels | CLI, tooling, or support maintainers | Review support docs, issue templates, and artifact-upload habits outside the repo before choosing a remediation shape | High |
| `tools/agent/run.py`, `tools/agent/trace.py`, `tools/agent/contract_stamp.py` | The path-bearing artifacts are verified, but the repo still does not prove which shared channels, if any, require a sanitized trace or stamp contract | The current trace and stamp shape is persisted, documented, and validated. That makes any future hardening change a contract-boundary decision, not a local cleanup. | `docs/security/logging-audit.md` confirmed path-bearing trace and stamp fields, and `docs/review/safe-fix-plan.md` recorded that the current artifact shape is already documented and validated | agent harness | Confirm whether any support, CI, or issue workflow actually needs a sanitized artifact contract before planning a change to the persisted trace or stamp format | High |
| `.github/pull_request_template.md` and out-of-repo branch protection or review policy | Template completion may still be reviewer-only even after the maintenance harness lands | The repo can validate the template text, but it cannot prove GitHub enforces completion before merge | `docs/review/maintenance-harness.md` and `docs/unknowns/maintenance-harness.md` both state that template completion is not hard-enforced in the repo | not declared in repo | Confirm whether branch protection, review policy, or maintainer workflow outside the repo treats template completion as required | Medium |
