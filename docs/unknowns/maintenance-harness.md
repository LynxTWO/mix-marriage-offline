<!-- markdownlint-disable-file MD013 -->

# Maintenance Harness Unknowns

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `.github/pull_request_template.md` | GitHub does not hard-enforce template completion in this repo | A contributor can still merge a PR with weak narrative fields unless reviewers push back | The repo has a PR template, but no local rule or GitHub setting in this repo forces completion before merge | not declared in repo | Confirm whether branch protection or review policy outside the repo treats template completion as required | Medium |
| `.github/workflows/release.yml`, `.github/workflows/pages.yml`, Windows signing and installer behavior | Out-of-repo release and publish steps still shape shipped behavior | The harness can remind reviewers about these paths, but it cannot prove GitHub-hosted runners, Pages deploy, signing tools, or installer behavior from repo code alone | The adversarial pass and scenario stress-test already marked release control planes and Pages publish as boundary-heavy paths | not declared in repo | Re-check these workflows when release or docs-publish behavior changes, and record any new out-of-repo dependency | High |
| `.claude/agents/`, `tools/sync_claude_agents.py` | Steering mirror drift still depends on a support sync path | A mirrored steering copy can look authoritative if it drifts from the repo source of truth | The repo treats `AGENTS.md` as authoritative, but `.claude/agents/` is still rewritten by a helper script | not declared in repo | Confirm whether the mirrored path needs a stronger warning or a dedicated review note when the sync path changes | Medium |
| `tools/validate_maintenance_harness.py` | The logging rule is intentionally narrow | Reviewers might mistake an obvious-drift grep for a complete telemetry or secret audit | The planned scan only catches same-line logging calls with obvious sensitive markers. It does not cover stderr forwarding, JSON stdout, trace uploads, or artifact sharing | not declared in repo | Keep `docs/security/logging-audit.md` current and extend review guidance before widening automation | High |
