# Logging Audit Unknowns

<!-- markdownlint-disable-file MD013 -->

This file records runtime questions from the logging, telemetry, and sensitive
data audit. Add entries here instead of guessing when code clearly forwards
stderr, logs, or trace artifacts but the exact payload depends on runtime
conditions.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `src/mmo/cli_commands/_project.py`, `src/mmo/cli_commands/_gui_rpc.py`, `gui/web/app.js` | Browser-side `project.show` hydration still lacks one manual refresh spot-check after the split between shell `json-shared` and GUI RPC `json` | The shell-safe profile now helps at the CLI boundary, but the browser still needs the full local RPC payload for artifact hydration | Static review shows the new CLI profile drops `project_dir` and `absolute_path`, while GUI RPC still calls `project.show` with `output_format="json"` and browser hydration still reads `absolute_path` receipts. Runtime probes now confirm the two transport boundaries too: `/api/rpc` returns the full local `project.show` payload with `project_dir` and per-artifact `absolute_path`, while bridge startup and CLI-runner failures only surface basename labels plus `stderr_present` or `stderr_lines` counts even when fake binaries emit path-rich stderr. | CLI or GUI maintainers | Run one browser refresh against the same project and confirm the UI panels hydrate from the full RPC payload without surfacing extra machine-local text elsewhere in the shell | `needs runtime confirmation` |
| `tools/smoke_packaged_desktop.py` and `.github/workflows/release.yml` | The shared console copy is now bounded, but the exact on-disk installer logs and Windows install-state artifact still depend on MSI or NSIS behavior and runner state | The leak moved from printed console output to local artifacts. That still matters if future support or release habits start attaching those files to shared channels. | Static review shows packaged smoke now prints labels and counts instead of raw installer output, and the release workflow now logs basename-only labels. The full installer logs and Windows install-state JSON still stay on disk for cleanup and signature checks. | desktop packaging or release tooling | Run the Windows packaged smoke flow once and inspect both the narrowed console output and the retained `msi-install.log`, `nsis-install.log`, or install-state JSON. | `needs runtime confirmation` |
| `tools/agent/*` | It is not confirmed whether path-bearing trace artifacts are ever shared outside local runs | The trace and contract-stamp files persist local paths. That is low risk locally, but it becomes a real leak if shared in CI artifacts, issue attachments, or support transcripts. | Static review found default writes to `sandbox_tmp/agent_trace.ndjson` and `.mmo_agent/graph_contract.json`, but no workflow upload was found in this repo. | agent harness | Confirm whether any manual or automated workflow exports or uploads these artifacts. | `needs runtime confirmation` |
