# Adversarial Pass Unknowns

<!-- markdownlint-disable-file MD013 -->

This file records the gaps that still need proof after the adversarial
edge-case review. Add rows here instead of stretching a clean code slice into a
claim that support tooling, release control planes, or out-of-repo behavior are
fully understood.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py` | The repo shape treats these as support tools, but it is still unclear how often they are used in CI, release prep, or maintainer workflows | They delete repo-local temp roots, delete stale packaged-data mirrors, or rewrite `.claude/agents/`. That is quiet control-plane behavior, not harmless glue code. | Static review shows explicit delete or copy operations and allowlist logic, but no single architecture note names them as first-class operational boundaries. | tooling or release maintainers | Trace where each script is invoked in docs, workflow jobs, and maintainer runbooks, then decide whether they need their own architecture note or review slice | high |
| `tools/run_renderers.py`, `tools/benchmark_render_precision.py`, `tools/capture_tauri_screenshots.py` | Direct render and screenshot helper entrypoints still sit outside the main CLI and desktop maps | These scripts import backend modules directly, write temp scenes or manifests, and can refresh committed screenshot evidence. They can change what maintainers treat as trusted review output. | Static review shows direct imports of validator or CLI code, temp-root writes, and screenshot baseline refresh guidance in the script text. | GUI or tooling maintainers | Confirm which of these scripts are CI-only, maintainer-only, or operator-facing, then document that boundary | medium |
| `.github/workflows/pages.yml`, `site/` | GitHub Pages publish is still under-described as a control-plane boundary | The workflow has `pages: write`, `id-token: write`, and a deploy job, so the repo does own a public publish path outside the local app runtime. | Static review confirms the deploy job exists, but the system map still treats Pages mostly as an operational note. | docs or release maintainers | Determine whether the site is fully hand-maintained or partly generated, and whether a separate publish-surface note is needed | medium |
| `.github/workflows/release.yml`, `tools/smoke_packaged_desktop.py` | The repo cannot prove exact Windows signer, installer, and receipt behavior from code alone | Release correctness depends on GitHub runner state, Windows cert store behavior, `signtool`, installer output, and smoke receipts that can differ by runtime environment. | Static review plus the logging audit show path-rich output and protected signing steps, but not a sanitized end-to-end release receipt. | desktop packaging or release tooling | Capture a Windows release dry run with sanitized receipts and decide which parts belong in repo docs versus runtime unknowns | high |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | It is still unclear which support or CI channels capture machine-readable JSON and trace artifacts outside local runs | That boundary determines whether local product output becomes shared telemetry and whether path-bearing traces remain local-only in practice. | The logging audit found path-rich JSON and trace files, but not a repo-owned workflow that clearly exports or forbids them. | CLI, tooling, or support maintainers | Review support docs, issue templates, and any artifact-upload habits that live outside the repo, then decide whether more redaction or documentation is needed | medium |
