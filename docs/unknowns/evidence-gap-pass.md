<!-- markdownlint-disable-file MD013 -->

# Evidence Gap Pass Unknowns

This file records the unresolved confidence gaps from the evidence-gap pass.
Add rows here instead of flattening support-tool, publish, release, or
output-boundary uncertainty into stronger claims than the repo can prove.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py` | Repo-local docs and git history now support human-run maintainer or contributor use for all three scripts, but the repo still cannot rule out an unwritten bootstrap or wrapper outside versioned files | That boundary decides whether the scripts stay as explicit maintainer tools or should be revisited as operational automation later | Static review now proves maintainer or contributor call sites in `docs/PROJECT_INSTRUCTIONS.md`, `docs/00-quickstart.md`, `docs/21-loudness-profiles.md`, `CLAUDE.md`, and `docs/contributing/ai-workflow.md`. Git history now matches that story. CI and release still do not call the scripts directly, though `sync_packaged_data_mirror.py` is enforced indirectly through `PKG.MIRROR` validation. | tooling or release maintainers | Treat the scripts as maintainer tools unless a future bootstrap note, setup helper, or workflow starts calling them directly | High |
| `tools/run_renderers.py`, `tools/benchmark_render_precision.py` | Git history now supports maintainer-local helper status for these entrypoints, but the repo still does not prove any broader trusted-evidence role | That boundary decides whether their outputs should stay local helper artifacts or be promoted into review evidence later | Static review confirms renderer execution, temp scene and request writes, and output hashing. The same pass now proves `capture_tauri_screenshots.py` is CI evidence and a maintainer baseline-refresh tool, and git history for the other two helpers does not show a workflow, release, or reviewer-facing promotion. | GUI or tooling maintainers | Leave them on the maintainer-local side unless a future workflow, README, or benchmark note names them as review evidence | Medium |
| `.github/workflows/pages.yml`, `site/` | Repo-local evidence now supports `site/` as a committed static payload that `pages.yml` uploads as-is, but the hosted Pages behavior still sits outside repo proof | The public docs site is shipped operator-facing behavior even when no repo-local site generator exists | Workflow inspection shows `pages.yml` uploads `site/` directly, the tracked tree is only `index.html`, `styles.css`, and `.nojekyll`, and git history shows direct edits rather than a repo-local build step | docs or release maintainers | Treat `site/` as committed static content unless a future workflow or build step starts generating it, and reopen the boundary only when deploy behavior changes | Medium |
| `.github/workflows/release.yml`, `tools/smoke_packaged_desktop.py` | The repo proves the release workflow order, artifact selection, smoke orchestration, install-state capture, and cleanup policy, but not the full Windows signing or installer runtime boundary | Release correctness still depends on GitHub-hosted runners, Windows cert-store behavior, `signtool`, Authenticode, timestamping, installer state, and hosted release behavior outside repo-owned code | Workflow inspection, smoke-harness source review, targeted tests, logging-audit notes, and git history now show the repo-owned Windows release path clearly, including optional signing, real install smoke, installed-signature verification, and bounded shared output | desktop packaging or release tooling | Keep separating repo-owned workflow facts from runner-, signer-, installer-, and hosted-release behavior, then capture a sanitized Windows dry run when that environment is available | High |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | The repo now proves the shell and GUI RPC `project.show` routes diverge as intended, one generic issue-report channel exists, no repo-owned workflow uploads these outputs, and shell-facing scan stdout now has the same shared-safe split, but it still does not prove how support or CI handles the explicit local contracts or the local artifacts in practice | That boundary decides when local product output becomes shared telemetry and when further redaction can happen safely without breaking intentional local-only use | Logging-audit and scenario-review docs already confirm project JSON, scan JSON, and agent trace artifacts carry path-bearing data. GUI RPC inspection plus local shell and RPC spot-checks prove `project.show --format json-shared` drops path fields while GUI RPC still returns the full local payload. The current branch now also proves that shell-facing `scan_session` defaults to a shared-safe profile while explicit `--format json-local` and `--out` keep the full local report. The bug template asks for exact commands and artifact paths, and CI and release workflow inspection found no upload path for these outputs. | CLI, tooling, or support maintainers | Confirm whether any out-of-repo support transcript, CI log habit, or issue-thread practice captures the explicit local `json-local`, file-backed scan reports, or agent artifacts before choosing a remediation shape | High |
| `tools/agent/run.py`, `tools/agent/trace.py`, `tools/agent/contract_stamp.py` | The current trace and stamp format is documented, validated, gitignored, and not uploaded by repo-owned workflows, but the repo still does not prove whether any shared human workflow needs a sanitized artifact contract | Any future hardening here changes a persisted artifact contract, not a local debug detail | The logging audit, safe-fix plan, `docs/agent_repl_harness.md`, `.gitignore`, and workflow inspection all confirm the current artifact shape is intentional, local by default, and not exported by repo-owned automation | agent harness | Confirm whether any support or review habit pastes these files into issue threads or PRs before planning a contract change | High |
| `.github/pull_request_template.md` and out-of-repo review policy | The template exists and is validated, but the repo still cannot prove that completion is enforced before merge | The maintenance harness could be overstated if reviewers assume the template is a hard gate | The repo has a real PR template and maintenance harness, but no repo-local branch-protection proof | not declared in repo | Check for any repo-local policy note that narrows the enforcement story before assuming review-only status is final | Medium |
