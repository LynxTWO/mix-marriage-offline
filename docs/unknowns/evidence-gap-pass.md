<!-- markdownlint-disable-file MD013 -->

# Evidence Gap Pass Unknowns

This file records the unresolved confidence gaps from the evidence-gap pass.
Add rows here instead of flattening support-tool, publish, release, or
output-boundary uncertainty into stronger claims than the repo can prove.

| Area or file | Concern | Why it matters | Evidence found so far | Likely owner if known | Next best check | Risk level |
| --- | --- | --- | --- | --- | --- | --- |
| `tools/safe_cleanup.py`, `tools/sync_packaged_data_mirror.py`, `tools/sync_claude_agents.py` | The scripts have verified control-plane side effects, but the repo still does not prove their real maintainer, CI, or release audience | Audience determines whether these should stay as support-tool notes or become first-class operational paths with stronger review rules | Static review confirms allowlisted deletes, packaged-data mirror rewrites, and synced steering-mirror rewrites, but not who runs them in practice | tooling or release maintainers | Trace each script through workflow jobs, README notes, and maintainer instructions still in the repo | High |
| `tools/run_renderers.py`, `tools/benchmark_render_precision.py`, `tools/capture_tauri_screenshots.py` | The helper entrypoints have verified side effects, but the repo still does not prove whether they are CI-only, maintainer-only, or operator-facing | That boundary decides whether their outputs count as trusted review evidence or local helper artifacts | Static review confirms renderer execution, temp scene and request writes, output hashing, and screenshot baseline refresh behavior | GUI or tooling maintainers | Confirm audience and intended evidence role from repo-local workflow and test docs | Medium |
| `.github/workflows/pages.yml`, `site/`, `.github/workflows/release.yml`, `tools/smoke_packaged_desktop.py` | The repo proves real publish and release control planes, but not the full out-of-repo runtime boundary | That gap blocks stronger claims about how docs publish, Windows signing, installer behavior, and smoke outputs behave end to end | Workflow inspection, system-map notes, and the logging audit all confirm these boundaries exist and carry real side effects | docs or release maintainers | Separate repo-owned facts from Pages, runner, signer, and installer behavior in a dedicated boundary note | High |
| `src/mmo/cli_commands/_project.py`, `src/mmo/tools/scan_session.py`, `tools/agent/*` | The repo now proves one local browser-visible `project.show` path, one generic issue-report channel, and no repo-owned workflow upload for these outputs, but it still does not prove how support or CI handles them in practice | That boundary decides when local product output becomes shared telemetry and when redaction can happen safely | Logging-audit and scenario-review docs already confirm project JSON, scan JSON, and agent trace artifacts carry path-bearing data. GUI RPC inspection proves `project.show` reaches the local browser shell, the bug template asks for exact commands and artifact paths, and CI and release workflow inspection found no upload path for these outputs. | CLI, tooling, or support maintainers | Confirm whether any out-of-repo support transcript, CI log habit, or issue-thread practice captures these outputs before choosing a remediation shape | High |
| `tools/agent/run.py`, `tools/agent/trace.py`, `tools/agent/contract_stamp.py` | The current trace and stamp format is documented, validated, gitignored, and not uploaded by repo-owned workflows, but the repo still does not prove whether any shared human workflow needs a sanitized artifact contract | Any future hardening here changes a persisted artifact contract, not a local debug detail | The logging audit, safe-fix plan, `docs/agent_repl_harness.md`, `.gitignore`, and workflow inspection all confirm the current artifact shape is intentional, local by default, and not exported by repo-owned automation | agent harness | Confirm whether any support or review habit pastes these files into issue threads or PRs before planning a contract change | High |
| `.github/pull_request_template.md` and out-of-repo review policy | The template exists and is validated, but the repo still cannot prove that completion is enforced before merge | The maintenance harness could be overstated if reviewers assume the template is a hard gate | The repo has a real PR template and maintenance harness, but no repo-local branch-protection proof | not declared in repo | Check for any repo-local policy note that narrows the enforcement story before assuming review-only status is final | Medium |
